# Day 03, Part 1 + Part 2
At each step in the iteration, we check (in parallel for each of the result digits) whether taking the current digit is both possible (enough remaining digits) and is larger than the corresponding current digit in the result.
